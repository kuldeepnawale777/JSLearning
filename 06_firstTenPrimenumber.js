function primeNumbers(startValue,totalPrime)
{ let count = 0;
     let num = startValue; 
     const arrayOfPrime = []; 
     while (count
        // ... Rest Parameter function show(arg1, ...arg){ console.log(arg1, arg); } show(3, 4, 6, 8, 9);